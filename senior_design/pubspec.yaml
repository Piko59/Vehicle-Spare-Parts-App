name: senior_design
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.31.1 # Correct version
  firebase_auth: ^4.7.1  # Correct version
  cloud_firestore: ^4.17.4
  firebase_storage: ^11.7.6
  image_picker: ^1.1.1
  cupertino_icons: ^1.0.6
  lottie: ^3.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - lib/images/
    - lib/animations/
    - assets/kaportaci1.jpg
    - assets/kaportaci2.jpg
    - assets/map_placeholder.png
